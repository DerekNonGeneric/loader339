import {
    dirname as pathDirName,
    resolve as resolvePath,
  } from 'path';
  import { fileURLToPath as pathFromUrl } from 'url';
  
  const __filename = pathFromUrl(import.meta.url);
  const __dirname = pathDirName(__filename);
  
  /** @type {string} */
  export const projectRoot = resolvePath('../../../', __dirname);
